# Client environment variables (Vite — must be prefixed with VITE_)
# In development: leave as localhost
# On Vercel: set to your Render backend URL (e.g. https://curtains-erp-api.onrender.com/api)
VITE_API_URL=http://localhost:4000/api
